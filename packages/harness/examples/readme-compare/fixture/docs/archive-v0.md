# Archived v0 notes

This document is intentionally stale and must not be used as current package
evidence. The removed `retry-kit` CLI used five attempts by default and exposed
a CommonJS `require()` entrypoint. Neither capability exists in version 1.0.
