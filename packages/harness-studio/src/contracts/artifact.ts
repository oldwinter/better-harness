/** The Artifact contract (Provider/adapter types) lives in the core SDK; re-exported here as Studio's single import point for both app and server code. */
export * from "@qoder-ai/harness/artifacts";
